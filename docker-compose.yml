services:
  mirofish:
    build: .
    container_name: mirofish
    env_file:
      - .env
    ports:
      - "3001:3000"
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads